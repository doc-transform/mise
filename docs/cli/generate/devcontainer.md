<!-- @generated by usage-cli from usage spec -->
# `mise generate devcontainer`

- **用法**: `mise generate devcontainer [FLAGS]`
- **源码**: [`src/cli/generate/devcontainer.rs`](https://github.com/jdx/mise/blob/main/src/cli/generate/devcontainer.rs)

生成用于执行 mise 的 devcontainer

## 标志

### `-i --image <IMAGE>`

devcontainer 使用的镜像

### `-m --mount-mise-data`

将 mise-data-volume 绑定挂载到 devcontainer

### `-n --name <NAME>`

devcontainer 的名称

### `-w --write`

写入 .devcontainer/devcontainer.json

示例：

```
mise generate devcontainer
```
